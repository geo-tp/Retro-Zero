#ifndef CP0_APP_UI_H
#define CP0_APP_UI_H

#include <string>

// Runs the launcher UI and returns the selected core path and ROM path.
bool run_app_ui(std::string &core_path, std::string &rom_path);

#endif // CP0_APP_UI_H
